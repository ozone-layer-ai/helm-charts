name: Release Charts

on:
  push:
    branches:
      - main

jobs:
  release:
    # depending on default permission settings for your org (contents being read-only or read-write for workloads), you will have to add permissions
    # see: https://docs.github.com/en/actions/security-guides/automatic-token-authentication#modifying-the-permissions-for-the-github_token
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"
      
      - name: Add all chart dependency repositories
        run: |
          for chart in charts/*; do
            if [ -f "$chart/Chart.yaml" ]; then
              repos=$(yq '.dependencies[].repository' "$chart/Chart.yaml")
              for repo in $repos; do
                # Build a stable, collision-resistant alias from the full URL.
                # Example: https://grafana.github.io/helm-charts -> grafana-github-io-helm-charts
                name=$(echo "$repo" | sed -E 's#^https?://##; s#^oci://##; s#[^a-zA-Z0-9]+#-#g; s#^-+##; s#-+$##' | tr '[:upper:]' '[:lower:]')
                helm repo add "$name" "$repo" || true
              done
              helm dependency update "$chart"
            fi
          done

      - name: Run chart-releaser
        uses: helm/chart-releaser-action@v1.7.0
        env:
          CR_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
